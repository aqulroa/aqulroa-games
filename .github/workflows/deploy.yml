name: Deploy Games to VPS
on:
  push:
    branches:
      - master  # <--- здесь должно быть master
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH Deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: root
          key: ${{ secrets.SSH_KEY }}
          port: 22
          script: |
            cd /var/www/aqulroa
            git pull origin master
            npm install
            pm2 restart aqulroa-games || pm2 start server.js --name "aqulroa-games"